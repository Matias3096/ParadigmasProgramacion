/*package org.example.clases;

import java.util.Random;

public class GeneradorAleatorio {
    private static final Random rnd = new Random();
    private static final String[] CALLES = {
            "San Martín", "Sarmiento", "Rivadavia", "Belgrano", "Colón",
            "Mitre", "Urquiza", "Brown", "Pellegrini", "Alem"
    };

    crea un lote con datos aleatorios
    public static Lote nextLote(){
        String dom = CALLES[rnd.nextInt(CALLES.length)] + " "+ (100 + rnd.nextInt(900));
        float $xm2 = 80000 + rnd.nextInt(12000);
        int m2 = 200 + rnd.nextInt(601);
        return new Lote(dom,$xm2,m2);
    }
}
*/